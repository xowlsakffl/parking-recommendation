<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>주차장 찾기</title>
    <!--부트스트랩 css 추가-->
    <link rel="stylesheet" href="/css/lib/bootstrap.min.css">

    <style>
        body {
            background-color: #f8f9fa; /* 회색 배경 */
            color: #343a40; /* 텍스트 색상 */
            font-family: 'Arial', sans-serif; /* 폰트 변경 */
        }
        .container {
            background-color: #ffffff; /* 흰색 배경 */
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }
        .h2-title {
            margin-bottom: 30px;
            font-size: 24px;
            font-weight: bold;
        }
        .table-hover tbody tr:hover {
            background-color: #e9ecef; /* 테이블 행 호버 색상 */
        }
        .table th, .table td {
            vertical-align: middle; /* 테이블 셀 수직 정렬 */
        }
        .grid-image, .h2-title {
            display: flex;
            justify-content: center;
        }
        img {
            margin: 0 0 5px;
            width: 70%;
            height: 550px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="h2-title">주차장 찾기 결과</div>

    <table class="table table-hover">
        <tr>
            <th>주차장명</th>
            <th>주차장 주소</th>
            <th>거리</th>
            <th>길안내 링크</th>
            <th>로드뷰 링크</th>
        </tr>
        {{#each outputFormList}}
            <tr>
                <td>{{parkingName}}</td>
                <td>{{parkingAddress}}</td>
                <td>{{distance}}</td>
                <td><a href="{{directionUrl}}">{{directionUrl}}</a></td>
                <td><a href="{{roadViewUrl}}">{{roadViewUrl}}</a></td>
            </tr>
        {{/each}}
    </table>
</div>

<script src="/js/lib/jquery.min.js"></script>
<script src="/js/lib/bootstrap.min.js"></script>

</body>
</html>
